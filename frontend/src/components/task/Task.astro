---
import FormTask from "./FormTask.astro";
---

<!-- Task component -->
<div class="bg-white/30 p-3 rounded-2xl">
  <!-- Search input -->
  <div class="bg-white/60 rounded-lg px-2 py-1">
    🔎
    <input
      type="text"
      placeholder="Search Tasks"
      class="outline-none caret-black/50"
    />
  </div>

  <!-- Add Task Form  -->
  <div class="flex cursor-pointer rounded-lg">
    <div
      class="flex flex-1 justify-between px-2 py-1 mt-3 cursor-pointer hover:bg-white/20 rounded-lg"
      id="btn-add-task"
    >
      <p>Add Task</p>
      <button class="text-lg">+</button>
    </div>
  </div>

  <FormTask />

  <!-- Tasks list -->
  <div class="flex justify-between px-2 py-4">
    <div>
      <input type="checkbox" name="task-check" id="" class="" />
      <span>Tarea 1</span>
    </div>
    <div>
      <!-- Acciones -->
      <button class=""> 🖋️ </button>
      <button class="">🩻</button>
    </div>
  </div>
</div>

<script>
  const btnAddTask = document.getElementById("btn-add-task");
  const container = document.getElementById("form-task-container");

  btnAddTask.addEventListener("click", () => {
    container.classList.toggle("invisible");
    container.classList.toggle("h-0");
    btnAddTask.classList.toggle("formOpen");
  });
</script>
